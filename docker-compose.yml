version: "3.9"

services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html:cached
      - ./vendor:/var/www/html/vendor
    environment:
      COMPOSER_ALLOW_SUPERUSER: 1
    restart: unless-stopped